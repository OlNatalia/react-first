import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// render App component into div with id root in index.html file
ReactDOM.render(<App />, document.getElementById('root'));